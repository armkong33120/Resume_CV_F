# 📸 คู่มือการอัพรูปภาพโปรไฟล์

## สำหรับหน้า /about

### 📸 ต้องการอัพรูปโปรไฟล์?

### 📁 ตำแหน่งไฟล์
```
portfolio/public/images/profile.jpg
```

### ✅ ไฟล์ที่รองรับ
- **JPG / JPEG** (แนะนำ - ขนาดไฟล์เล็ก)
- **PNG**
- **WebP**
- **GIF**

### 📐 สเปครูปภาพ

#### อัตราส่วน (Aspect Ratio)
- **1:1** (สี่เหลี่ยมจัตุรัส) - **แนะนำ**

#### ขนาด (Dimensions)
- **ความกว้าง**: 800px - 1200px
- **ความสูง**: 800px - 1200px
- **ตัวอย่าง**: 1000x1000px

#### ขนาดไฟล์ (File Size)
- **สูงสุด**: 2MB
- **แนะนำ**: 500KB - 1MB

### 📝 ขั้นตอนการอัพโหลด
- อัพโหลดไฟล์ชื่อ **"profile.jpg"** ไปที่โฟลเดอร์ **public/images/**
- 📁 `public/images/profile.jpg`

### 🎨 ข้อแนะนำ
1. ใช้รูปโปรไฟล์ที่ชัดเจน เห็นใบหน้าชัด
2. พื้นหลังเรียบหรือง่าย ไม่รบกวน
3. ไฟล์ .jpg มีขนาดเล็กกว่า .png
4. ตรวจสอบว่าไฟล์ไม่มีข้อผิดพลาด

### 📝 วิธีอัพโหลด

#### วิธีที่ 1: ใช้ File Explorer
1. เตรียมรูปภาพตามสเปค
2. เปิดโฟลเดอร์ `portfolio/public/images/`
3. ลากไฟล์รูปภาพมาวาง
4. เปลี่ยนชื่อไฟล์เป็น `profile.jpg`

#### วิธีที่ 2: ใช้ PowerShell
```powershell
# คัดลอกไฟล์ไปที่โฟลเดอร์ images
Copy-Item "C:\path\to\your\photo.jpg" -Destination "portfolio\public\images\profile.jpg"

# ตัวอย่าง
Copy-Item "C:\Users\Lab_test\Pictures\my-photo.jpg" -Destination "portfolio\public\images\profile.jpg"
```

#### วิธีที่ 3: ใช้ Command Prompt
```cmd
copy "C:\path\to\your\photo.jpg" "portfolio\public\images\profile.jpg"
```

### 🔄 หลังจากอัพโหลด
1. **ถ้า dev server กำลังรันอยู่**: 
   - Refresh หน้าเว็บที่ `/about`
   - หรือรีสตาร์ท dev server: กด `Ctrl+C` แล้วรัน `npm run dev` อีกครั้ง

2. **ถ้า build production**:
   ```powershell
   npm run build
   npm run start
   ```

### 🖼️ ตัวอย่างโครงสร้างโฟลเดอร์
```
portfolio/
└── public/
    └── images/
        ├── profile.jpg          ← อัพโหลดรูปโปรไฟล์ที่นี่
        └── projects/            ← รูปโปรเจกต์ (ถ้ามี)
            ├── ecommerce-cover.jpg
            └── healthcare-cover.jpg
```

### ❓ ถ้าไม่เห็นรูปภาพ

1. **ตรวจสอบชื่อไฟล์**: ต้องเป็น `profile.jpg` หรือ `profile.png` เท่านั้น
2. **ตรวจสอบตำแหน่ง**: ต้องอยู่ใน `public/images/` เท่านั้น
3. **รีสตาร์ท dev server**: กด `Ctrl+C` แล้วรัน `npm run dev` อีกครั้ง
4. **ล้าง cache**: กด `Ctrl + Shift + R` ในเบราว์เซอร์

### 📸 สำหรับรูปภาพโปรเจกต์

ถ้าต้องการอัพรูปภาพโปรเจกต์ ให้อัพโหลดไปที่:
```
public/images/projects/
```

และอ้างอิงในไฟล์ `content/projects.json` เช่น:
```json
{
  "coverImage": "/images/projects/my-project-cover.jpg",
  "galleryImages": [
    "/images/projects/my-project-1.jpg",
    "/images/projects/my-project-2.jpg"
  ]
}
```

---

**💡 Tip**: Next.js จะ optimize รูปภาพอัตโนมัติเมื่อใช้ `next/image` component

