# 🚀 วิธีรัน Portfolio Website

## ข้อกำหนดเบื้องต้น
- Node.js เวอร์ชัน 18 หรือสูงกว่า (ดาวน์โหลด: https://nodejs.org/)

---

## 📋 ขั้นตอนการรัน (3 ขั้นตอนง่ายๆ)

### 1️⃣ ติดตั้ง Dependencies (ทำครั้งแรกเท่านั้น)
เปิด PowerShell ในโฟลเดอร์ `portfolio` แล้วรัน:

```powershell
npm install
```

รอให้ติดตั้งเสร็จ (ประมาณ 2-5 นาที)

---

### 2️⃣ เริ่มเซิร์ฟเวอร์ Development

```powershell
npm run dev
```

คุณจะเห็นข้อความแบบนี้:
```
✓ Ready in 2.5s
○ Local:        http://localhost:3000
```

---

### 3️⃣ เปิดใน Browser

เปิดเบราว์เซอร์ (Chrome, Edge, Firefox) แล้วไปที่:

**👉 http://localhost:3000**

---

## 🛑 หยุดเซิร์ฟเวอร์

กด `Ctrl + C` ใน PowerShell

---

## 🔧 คำสั่งอื่นๆ ที่มี

- **`npm run build`** - สร้าง Production build
- **`npm run start`** - รัน Production server (ต้อง build ก่อน)
- **`npm run lint`** - ตรวจสอบโค้ด
- **`npm test`** - ทดสอบ API endpoints

---

## ❓ ปัญหาที่พบบ่อย

### Port 3000 ถูกใช้งานอยู่แล้ว?
```powershell
# หยุด process ที่ใช้ port 3000
Get-NetTCPConnection -LocalPort 3000 | Select-Object -ExpandProperty OwningProcess | ForEach-Object { Stop-Process -Id $_ -Force }
```

### ต้องการใช้ port อื่น?
```powershell
$env:PORT=3001; npm run dev
```

---

**Happy Coding! 🎉**

