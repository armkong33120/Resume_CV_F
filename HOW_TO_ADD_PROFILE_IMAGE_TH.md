# 📸 วิธีใส่รูปโปรไฟล์ - คู่มือง่ายๆ

## 🎯 ภาพรวม

รูปโปรไฟล์จะแสดงในหน้า **/about** ของเว็บไซต์

## 📁 ตำแหน่งไฟล์ที่ต้องอัพโหลด

```
portfolio/
└── public/
    └── images/
        └── profile.jpg    ← ใส่รูปที่นี่
```

## ✅ ขั้นตอนง่ายๆ

### วิธีที่ 1: ใช้ File Explorer (ง่ายที่สุด)

1. **เตรียมรูปภาพ**
   - เปิดไฟล์รูปที่ต้องการ
   - ถ้าต้องการ ปรับขนาดเป็น 1:1 (สี่เหลี่ยมจัตุรัส)
   - เปลี่ยนชื่อไฟล์เป็น `profile.jpg`

2. **อัพโหลดไฟล์**
   - เปิดโฟลเดอร์ `portfolio/public/images/`
   - ลากไฟล์ `profile.jpg` มาวางในโฟลเดอร์นี้

3. **รีเฟรชหน้าเว็บ**
   - เปิดหน้า `/about` ในเบราว์เซอร์
   - กด `Ctrl + Shift + R` (Windows) หรือ `Cmd + Shift + R` (Mac) เพื่อรีเฟรช

### วิธีที่ 2: ใช้ Command Line (macOS/Linux)

```bash
# ไปที่โฟลเดอร์โปรเจกต์
cd /Users/pattharawadee/Desktop/portfolio

# คัดลอกไฟล์รูปภาพ
cp /path/to/your/photo.jpg public/images/profile.jpg

# ตัวอย่าง
cp ~/Pictures/my-photo.jpg public/images/profile.jpg
```

### วิธีที่ 3: ใช้ Terminal (macOS)

```bash
# เปิด Terminal
# ไปที่โฟลเดอร์โปรเจกต์
cd ~/Desktop/portfolio

# คัดลอกไฟล์รูปภาพ
cp ~/Downloads/photo.jpg public/images/profile.jpg
```

## 📐 สเปครูปภาพที่แนะนำ

### ขนาดรูปภาพ
- **อัตราส่วน**: 1:1 (สี่เหลี่ยมจัตุรัส) - **แนะนำ**
- **ขนาด**: 800px × 800px ถึง 1200px × 1200px
- **ตัวอย่าง**: 1000px × 1000px

### รูปแบบไฟล์
- ✅ **JPG / JPEG** (แนะนำ - ขนาดไฟล์เล็ก)
- ✅ **PNG**
- ✅ **WebP**

### ขนาดไฟล์
- **สูงสุด**: 2MB
- **แนะนำ**: 500KB - 1MB

## 🎨 ข้อแนะนำ

1. **ใช้รูปโปรไฟล์ที่ชัดเจน** - เห็นใบหน้าชัดเจน
2. **พื้นหลังเรียบ** - ไม่รบกวนการมองเห็น
3. **ไฟล์ JPG** - มีขนาดเล็กกว่า PNG
4. **ปรับขนาดก่อน** - ลดขนาดไฟล์ให้เหมาะสม

## 🔄 หลังจากอัพโหลดรูปแล้ว

### ถ้า Dev Server กำลังรันอยู่:
1. **Refresh หน้าเว็บ** - เปิด `/about` แล้วกด `Ctrl + Shift + R`
2. **หรือรีสตาร์ท Server**:
   ```bash
   # กด Ctrl + C เพื่อหยุด server
   # แล้วรันใหม่
   npm run dev
   ```

### ถ้าไม่เห็นรูปภาพ:

1. **ตรวจสอบชื่อไฟล์**
   - ✅ ต้องเป็น `profile.jpg` หรือ `profile.png`
   - ❌ ไม่ใช่ `Profile.jpg` หรือ `profile.JPG` (case sensitive บางระบบ)

2. **ตรวจสอบตำแหน่ง**
   - ✅ ต้องอยู่ที่ `public/images/profile.jpg`
   - ❌ ไม่ใช่ `public/profile.jpg` หรือ `images/profile.jpg`

3. **รีสตาร์ท Dev Server**
   ```bash
   # กด Ctrl + C เพื่อหยุด
   npm run dev
   ```

4. **ล้าง Cache**
   - กด `Ctrl + Shift + R` (Windows/Linux)
   - กด `Cmd + Shift + R` (Mac)

## 📱 วิธีปรับขนาดรูปภาพ (ถ้าต้องการ)

### ใช้ Online Tool:
1. ไปที่ [ResizeImage.net](https://resizeimage.net/) หรือ [TinyPNG](https://tinypng.com/)
2. อัพโหลดรูป
3. ปรับขนาดเป็น 1000×1000px
4. ดาวน์โหลดและใช้ไฟล์ใหม่

### ใช้ macOS Preview:
1. เปิดรูปใน Preview
2. กด `Tools` → `Adjust Size`
3. ใส่ 1000 × 1000 pixels
4. Save

## 🖼️ ตัวอย่างโครงสร้าง

```
portfolio/
└── public/
    └── images/
        ├── profile.jpg          ← รูปโปรไฟล์ของคุณ
        ├── profile.png          ← หรือใช้ PNG ก็ได้
        └── projects/            ← รูปโปรเจกต์ (ถ้ามี)
```

## ✅ Checklist

- [ ] เตรียมรูปภาพ (1:1, 800-1200px)
- [ ] เปลี่ยนชื่อเป็น `profile.jpg`
- [ ] อัพโหลดไปที่ `public/images/`
- [ ] Refresh หน้า `/about`
- [ ] ตรวจสอบว่ามีรูปภาพแสดง

## 💡 Tips

- **ถ้ามีรูปหลายรูป**: ใช้รูปที่ชัดเจนที่สุด
- **ขนาดไฟล์ใหญ่เกินไป**: ใช้ [TinyPNG](https://tinypng.com/) เพื่อลดขนาด
- **รูปไม่เป็นสี่เหลี่ยม**: ใช้ [Canva](https://www.canva.com/) หรือ [Photopea](https://www.photopea.com/) ปรับขนาด

---

**พร้อมแล้ว!** เพียงแค่อัพโหลดไฟล์ `profile.jpg` ไปที่ `public/images/` แล้ว refresh หน้าเว็บ 🎉

