# 🌐 วิธีเข้าถึงเว็บจาก IP ใน LAN (Local Network)

## 📋 วัตถุประสงค์
ให้สามารถเข้าถึงเว็บ Portfolio จากอุปกรณ์อื่นๆ ในเครือข่าย LAN เดียวกัน (Wi-Fi เดียวกัน) ได้

---

## 🔍 ขั้นตอนที่ 1: ดู IP Address ของเครื่อง

### Windows (PowerShell)
```powershell
# วิธีที่ 1: ใช้ ipconfig
ipconfig

# ดูที่ "IPv4 Address" ภายใต้ "Wireless LAN adapter Wi-Fi" หรือ "Ethernet adapter"
# ตัวอย่าง: 192.168.1.100

# วิธีที่ 2: ดูแบบละเอียด
ipconfig /all

# วิธีที่ 3: ใช้ Get-NetIPAddress (PowerShell)
Get-NetIPAddress -AddressFamily IPv4 | Where-Object {$_.InterfaceAlias -notlike "*Loopback*"} | Select-Object IPAddress, InterfaceAlias
```

### Windows (Command Prompt)
```cmd
ipconfig
```

**ตัวอย่างผลลัพธ์:**
```
Wireless LAN adapter Wi-Fi:
   IPv4 Address. . . . . . . . . . . : 192.168.1.100
   Subnet Mask . . . . . . . . . . . : 255.255.255.0
   Default Gateway . . . . . . . . . : 192.168.1.1
```

**บันทึก IP Address:** `192.168.1.100` (ตัวอย่าง - ใช้ IP ของคุณ)

---

## 🚀 ขั้นตอนที่ 2: รัน Next.js ให้รองรับการเข้าถึงจาก LAN

### วิธีที่ 1: ใช้ -H หรือ --hostname

```powershell
cd portfolio
npm run dev -- -H 0.0.0.0
```

หรือ

```powershell
cd portfolio
npm run dev -- --hostname 0.0.0.0
```

**ผลลัพธ์:**
```
  ▲ Next.js 14.x.x
  - Local:        http://localhost:3000
  - Network:      http://192.168.1.100:3000  ← ใช้ URL นี้ในอุปกรณ์อื่น
```

### วิธีที่ 2: ตั้งค่าใน package.json (แนะนำ)

แก้ไขไฟล์ `package.json`:

```json
{
  "scripts": {
    "dev": "next dev -H 0.0.0.0",
    "dev:local": "next dev",
    "build": "next build",
    "start": "next start -H 0.0.0.0",
    "lint": "next lint"
  }
}
```

แล้วรัน:
```powershell
npm run dev
```

### วิธีที่ 3: ใช้ Environment Variable

สร้างไฟล์ `.env.local` ในโฟลเดอร์ `portfolio`:

```
HOSTNAME=0.0.0.0
PORT=3000
```

---

## 📱 ขั้นตอนที่ 3: เข้าถึงจากอุปกรณ์อื่น

### จากคอมพิวเตอร์เครื่องอื่นในเครือข่ายเดียวกัน:
1. เปิดเบราว์เซอร์ (Chrome, Edge, Safari, Firefox)
2. ไปที่: `http://192.168.1.100:3000`
   - แทน `192.168.1.100` ด้วย IP Address ของเครื่องคุณ

### จากมือถือ/แท็บเล็ต (iOS/Android):
1. เชื่อมต่อ Wi-Fi เดียวกับคอมพิวเตอร์
2. เปิดเบราว์เซอร์ (Safari, Chrome)
3. ไปที่: `http://192.168.1.100:3000`

### ตัวอย่าง URLs:
- `http://192.168.1.100:3000`
- `http://192.168.0.105:3000`
- `http://10.0.0.50:3000`

---

## 🔒 ขั้นตอนที่ 4: ปิด Firewall (ถ้าจำเป็น)

### Windows Firewall

#### วิธีที่ 1: อนุญาตผ่าน Windows Security
1. เปิด **Windows Security** (กด `Windows + I` → **Privacy & Security** → **Windows Security**)
2. คลิก **Firewall & network protection**
3. คลิก **Allow an app through firewall**
4. คลิก **Change settings** → **Allow another app**
5. เพิ่ม **Node.js** หรือ **npm** หรือ **Port 3000**

#### วิธีที่ 2: ใช้ PowerShell (Run as Administrator)
```powershell
# อนุญาต Port 3000
New-NetFirewallRule -DisplayName "Next.js Dev Server" -Direction Inbound -Protocol TCP -LocalPort 3000 -Action Allow
```

#### วิธีที่ 3: ใช้ Windows Defender Firewall GUI
1. ค้นหา "Windows Defender Firewall with Advanced Security"
2. คลิก **Inbound Rules** → **New Rule**
3. เลือก **Port** → **Next**
4. เลือก **TCP** → ใส่ **3000** → **Next**
5. เลือก **Allow the connection** → **Next**
6. เลือก Domain, Private, Public ทั้งหมด → **Next**
7. ตั้งชื่อ "Next.js Dev Server" → **Finish**

---

## 🧪 ทดสอบการเชื่อมต่อ

### จากเครื่องเดียวกัน (Localhost)
```
http://localhost:3000
```

### จากอุปกรณ์อื่น (LAN)
```
http://[IP-Address]:3000
```

### ตรวจสอบว่า Server เปิดอยู่:
```powershell
# ตรวจสอบ Port 3000 ว่าถูกใช้งานอยู่
netstat -ano | findstr :3000
```

---

## ❓ แก้ปัญหา

### ปัญหา: เข้าไม่ได้จากอุปกรณ์อื่น

1. **ตรวจสอบ IP Address:**
   ```powershell
   ipconfig
   ```
   ตรวจสอบว่า IP ไม่เปลี่ยน (ถ้าใช้ DHCP อาจเปลี่ยนได้)

2. **ตรวจสอบว่าใช้ -H 0.0.0.0:**
   ```powershell
   npm run dev -- -H 0.0.0.0
   ```
   ต้องเห็น "Network: http://[IP]:3000"

3. **ตรวจสอบ Firewall:**
   - ตรวจสอบว่า Firewall ไม่บล็อก Port 3000
   - ลองปิด Firewall ชั่วคราวเพื่อทดสอบ

4. **ตรวจสอบ Network:**
   - ตรวจสอบว่าอุปกรณ์เชื่อมต่อ Wi-Fi เดียวกัน
   - ตรวจสอบว่าไม่ได้ใช้ VPN ที่อาจเปลี่ยน Network

5. **ตรวจสอบ Router/Network Settings:**
   - บาง Router อาจมี AP Isolation ที่ป้องกันการเชื่อมต่อระหว่างอุปกรณ์
   - ตรวจสอบใน Router Settings

### ปัญหา: IP Address เปลี่ยนทุกครั้ง

ถ้า IP Address เปลี่ยนทุกครั้งที่เปิดเครื่อง (เพราะใช้ DHCP):

**วิธีแก้: ตั้งค่า Static IP**

1. เปิด **Network Settings** (`Windows + I` → **Network & Internet**)
2. คลิก **Properties** ของ Wi-Fi/Ethernet
3. เลื่อนลงไปหา **IP assignment** → **Edit**
4. เปลี่ยนเป็น **Manual**
5. ตั้งค่า:
   - IP address: `192.168.1.100` (ใช้ IP ที่ต้องการ)
   - Subnet mask: `255.255.255.0`
   - Gateway: `192.168.1.1` (ดูจาก `ipconfig`)
   - DNS: `8.8.8.8` และ `8.8.4.4`

---

## 🔐 ความปลอดภัย

⚠️ **คำเตือน:**
- การเปิด Server ให้เข้าถึงได้จาก LAN อาจทำให้คนอื่นในเครือข่ายเดียวกันเข้าถึงได้
- ใช้เฉพาะในเครือข่ายที่เชื่อถือได้ (Home/Office Network)
- อย่าเปิดในเครือข่ายสาธารณะ (Public Wi-Fi)

---

## 📝 สรุปคำสั่งที่ใช้บ่อย

### Development (เข้าถึงได้จาก LAN)
```powershell
cd portfolio
npm run dev -- -H 0.0.0.0
```

### Production (เข้าถึงได้จาก LAN)
```powershell
cd portfolio
npm run build
npm run start -- -H 0.0.0.0
```

### ดู IP Address
```powershell
ipconfig
# หรือ
Get-NetIPAddress -AddressFamily IPv4 | Where-Object {$_.InterfaceAlias -notlike "*Loopback*"}
```

---

**💡 Tip**: ใช้ QR Code Generator เพื่อสร้าง QR Code ที่ชี้ไปที่ `http://[IP]:3000` เพื่อให้สะดวกในการแชร์ URL ไปยังมือถือ

